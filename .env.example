# Supabase connection (Nuxt Supabase module reads these at runtime)
# Replace with your project’s values from https://app.supabase.com
SUPABASE_URL="https://YOUR-PROJECT-REF.supabase.co"
SUPABASE_KEY="YOUR_ANON_PUBLIC_KEY"

# Drizzle / Postgres connection string (used on server-side only)
# For Supabase, copy the "Connection string" for client libraries (Node) and keep sslmode=require
DATABASE_URL="postgres://postgres:YOUR_PASSWORD@YOUR_HOST:6543/postgres?sslmode=require"

# Node environment (optional)
# NODE_ENV=development
